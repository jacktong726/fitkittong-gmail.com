server:
  port: 9001
spring:
  application:
    name: tensquare-base
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://centos7.jacktong726.com:3306/tensquare_base?characterEncoding=utf-8
    username: root
    password: 123456
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true
  rabbitmq:
    host: alicloud.jacktong726.com
  cloud:
    config:
      discovery:
        enabled: true
        service-id: TENSQUARE-CONFIG
      profile: ${spring.profiles.active:default}
    bus:
      enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://alicloud.jacktong726.com:6868/eureka
  instance:
    prefer-ip-address: true
myvalue: 123456
